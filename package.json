{
  "name": "bootstrap-customization",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass src/scss -o css && node-sass src/scss -o css -watch",
    "live-server": "live-server --port=3000 --open=./src/html --mount=node_modules:../../node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.1.3",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.4"
  },
  "devDependencies": {
    "node-sass": "^4.10.0"
  }
}
